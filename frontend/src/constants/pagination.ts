/** Tamaño de página por defecto en listados (alineado con el backend). */
export const DEFAULT_LIST_PAGE_SIZE = 10;

/** Máximo habitual en `PaginationDto` del API (usuarios, pacientes, citas, etc.). */
export const LIST_PAGE_SIZE_MAX = 100;

/** El catálogo de tratamientos permite hasta 500 por petición. */
export const TREATMENTS_PAGE_SIZE_MAX = 500;
